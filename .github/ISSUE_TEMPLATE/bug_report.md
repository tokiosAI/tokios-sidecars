---
name: Bug report
about: Report a problem with a Tokios sidecar
title: ''
labels: bug
assignees: ''
---

**Which sidecar and version**
`tokios-claude-sidecar` or `tokios-chatgpt-sidecar`, and the release tag (or the downloaded
file name / the binary's file version):

**CLI and version**
Output of `claude --version` or `codex --version`:

**Operating system**
(e.g. Ubuntu 24.04 / Windows 11 / macOS 15 on arm64)

**What happened**
A clear description of the bug and what you expected instead. If the same prompt works when
you run the CLI directly in a terminal, say so.

**Steps to reproduce**
1.
2.
3.

**Relevant log lines**
Logs never contain prompts or responses (see SECURITY.md), so they are safe to paste — but
please skim before posting. Add `--Logging:LogLevel:Default=Debug` for more detail.

```
(paste logs here)
```

<!-- Do NOT report suspected security vulnerabilities here. See SECURITY.md. -->
